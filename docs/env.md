# Environment variables

## Backend
- `DATABASE_URL=postgresql+psycopg://app:app@localhost:5432/app` - PostgreSQL connection URL
- `STORAGE_ROOT=backend/storage` - Root directory for file storage (contracts, PDFs)

## Frontend
- (none yet)
